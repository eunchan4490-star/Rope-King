# Rope-King 작업 규칙

- 사용자가 게임 수정을 요청하면 먼저 로컬에서 수정하고 Godot headless 실행과 `git diff --check`로 검증한다.
- 사용자가 수정 후 "설치할 수 있게 해", "깃허브에 올려", "APK 만들어"처럼 배포를 명시한 경우에만 커밋과 원격 push를 진행한다.
- 배포 요청을 받으면 이번 작업에서 수정한 파일만 골라 `tools/publish-android.ps1`의 `-Files`에 전달한다. 사용자 파일이나 관련 없는 변경사항은 함께 커밋하지 않는다.
- 스크립트가 GitHub Actions의 `Android APK` 빌드 완료를 확인하고 APK를 내려받을 때까지 기다린다. 성공 후 실행 링크와 APK 경로를 사용자에게 알려준다.
- 토큰, 키, 서명 파일, `builds/` 산출물은 커밋하지 않는다.
