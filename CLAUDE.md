# 줄넘킹 AI 협업 규칙

먼저 `AGENTS.md`를 읽고, 아래 규칙을 함께 따른다.

## 역할

- Claude Code: 기본 구현, 관련 파일만 수정, 테스트, 최종 통합·배포 담당.
- Codex: 중요한 다중 파일 변경·줄 판정·협동 입력·저장·출시 전 위험만 읽기 전용 검토.
- 사용자: 기능 방향, 화면 취향, 휴대폰 확인, 커밋·배포 승인 담당.

## 작업 규칙

- 작은 UI·문구·에셋 변경에는 Codex를 호출하지 않는다.
- 필요할 때만 `/codex:review --background`를 기능당 1회 호출한다.
- 설계 위험은 `/codex:adversarial-review --background 위험 지점`, 반복 실패는 `/codex:rescue --background 해결 목표`를 사용한다.
- 자동 리뷰 게이트는 사용하지 않는다.
- Claude와 Codex가 같은 파일을 동시에 수정하지 않는다.
- 사용자가 `커밋` 또는 `배포`라고 하기 전에는 push·APK 빌드를 하지 않는다.
- 전체 기획과 대화를 반복해서 읽지 말고 현재 목표와 관련 파일만 확인한다.

## 줄넘킹 고정 규칙

- 보이는 줄 통과 시점과 실제 성공 판정은 같은 규칙을 사용한다.
- 난이도는 속도뿐 아니라 엇박자·급가속·물결 리듬으로 변화시킨다.
- 협동 모드에서 왼쪽 터치는 왼쪽 플레이어만 점프시킨다.
- 협동 모드는 한 명이라도 걸리면 게임 오버다.
- 각 플레이어 위치에서 줄 높이를 계산한다.
- 최신 사용자의 결정이 이전 기획보다 우선한다.

## 사용자 요청 형식

목표: 한 가지 기능
규칙: 반드시 유지할 것
완료: 테스트 또는 APK 등 결과

## 보고 형식

완료 / 테스트 결과 / 사용자 휴대폰 확인 항목 / 커밋·APK 정보만 짧게 보고한다.

Notion 운영 허브: https://app.notion.com/p/3c9918abda0c812685e4e01c23a286d1
